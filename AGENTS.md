# CodeArchive 멀티에이전트 운영 계약

이 문서는 CodeArchive 저장소에서 활동하는 모든 에이전트의 공통 규칙이다.
에이전트는 작업을 시작하기 전에 이 문서와 자신의 역할 카드를 읽어야 한다.

## 1. 운영 목표

작업은 `기획 -> 구현 -> 검증 -> 기록 -> 기획 종료` 순서로 진행한다.
각 단계는 앞 단계의 명시적인 산출물을 입력으로 사용하며, 검증 증거와 문서 반영 없이
작업을 완료로 표시하지 않는다.

## 2. 소스 오브 트루스

요구사항이나 구현 방향이 충돌하면 다음 우선순위를 적용한다.

1. 사용자가 이번 작업에서 직접 내린 지시
2. 승인된 `project.md`의 현재 목표, 범위, 일정, 결정 사항
3. 승인된 ADR
4. `docs/proposal/codearchive-proposal.md`의 MVP 범위, 개발 단계, 성공 기준
5. `docs/architecture.md`, `docs/data-model.md`, `docs/security-policy.md`,
   `docs/conventions.md`
6. 현재 코드와 테스트가 표현하는 동작

상위 문서와 하위 문서 또는 코드가 충돌하면 임의로 해석하지 않는다. 기획 역할에 변경
요청을 전달하고, 결정이 필요한 부분은 범위에서 제외하거나 명시적으로 차단 상태로 둔다.
`project.md`가 아직 없다면 proposal을 임시 기준으로 사용하되, 일정과 완료 상태를 추정해
기록하지 않는다.

## 3. 역할과 역할 카드

- 기획: `.agents/roles/planning.md`
- 구현: `.agents/roles/implementation.md`
- 검증: `.agents/roles/verification.md`
- 기록: `.agents/roles/records.md`

한 작업에서 역할을 겸해야 한다면 단계별로 역할을 명시하고, 구현자가 자신의 구현을 최종
승인하지 않는다. 독립 검증이 불가능한 경우 그 사실을 검증 보고서와 최종 보고에 남긴다.

## 4. 작업 단위

모든 변경은 `TASK-####` 식별자를 가진 작업 브리프에서 시작한다. 작업 브리프에는 다음
항목이 있어야 한다.

- 연결된 GitHub Issue 번호와 URL
- 배경과 사용자 가치
- proposal, project 또는 ADR에 대한 추적 근거
- 범위와 비범위
- 관찰 가능한 인수 조건
- 필요한 자동 및 수동 테스트
- 의존성, 일정, 위험
- 데이터 스키마, 마이그레이션, 보안, Chrome 권한, 외부 전송 영향
- README, 아키텍처, 데이터 모델, ADR, KPI 등 문서 영향
- 변경 파일의 예상 소유자

인수 조건이 모호하거나 보안 및 데이터 영향이 결정되지 않은 작업은 구현 준비 상태가
아니다.

## 5. 파일 소유권과 동시 작업

기본 소유권은 다음과 같다.

| 영역                                                    | 기본 소유 역할 |
| ------------------------------------------------------- | -------------- |
| `project.md`, 로드맵, 백로그, 변경 승인                 | 기획           |
| `src/**`, `public/**`                                   | 구현           |
| `tests/**`, 테스트 fixture, 검증 보고                   | 검증           |
| `README.md`, `docs/**`, ADR, KPI, 기술 스택, 트러블슈팅 | 기록           |

다음은 공유 파일이므로 작업 브리프에서 단 한 명의 수정 소유자를 지정한다.

- `package.json`, lockfile
- `vite.config.ts`, `tsconfig*.json`, ESLint 및 Prettier 설정
- `public/manifest.json`
- `.github/**`, `.husky/**`, commitlint 설정
- 루트 운영 문서와 공통 타입 또는 공통 스키마

공유 파일을 동시에 수정하지 않는다. 소유자가 아닌 역할은 필요한 변경을 handoff에
제안한다. 병렬 작업은 서로 겹치지 않는 파일 범위에서만 수행하며, 가능하면 역할별 branch
또는 worktree를 사용한다. 다른 에이전트의 변경을 되돌리거나 덮어쓰지 않는다.

## 6. 핸드오프 흐름

### 기획에서 구현으로

기획은 작업 식별자, 범위, 인수 조건, 비범위, 일정, 의존성, KPI 측정법, 보안 및 ADR
필요 여부를 전달한다.

### 구현에서 검증으로

구현은 다음을 전달한다.

- 변경 파일과 동작 요약
- 인수 조건별 구현 위치
- 선택한 설계와 대안
- 실행한 focused 검사와 결과
- 알려진 위험, 미완료 항목, 수동 확인 방법
- 데이터, 권한, 외부 통신, 문서 영향

### 검증에서 구현 또는 기록으로

검증 실패 시 결함 식별자, 재현 절차, 기대/실제 결과, 환경, 심각도, 관련 인수 조건을
구현에 반환한다. 통과 시 명령과 결과, 인수 조건별 증거, 자동/수동 검증 구분, 잔여 위험을
기록에 전달한다.

### 기록에서 기획으로

기록은 실제 변경한 README, 아키텍처, 데이터 모델, ADR, 기술 스택, 트러블슈팅, KPI와
각 근거를 전달한다. 측정하지 않은 KPI를 달성으로 기록하지 않는다.

### 종료

기획은 인수 조건 충족, 검증 통과, 필수 문서 반영, 미해결 위험의 명시를 확인한 뒤에만
작업을 완료로 표시한다.

## 7. 공통 품질 게이트

기존 검증 체인을 최종 표준으로 사용한다.

```bash
npm ci
npm run validate
```

`npm run validate`의 lint, format check, typecheck, Vitest, production build를 임의로
생략하거나 중복 스크립트로 대체하지 않는다. 개발 중에는 변경 범위에 맞는 focused test를
먼저 실행할 수 있지만 최종 handoff에는 전체 검증 결과가 필요하다.

다음 조건도 충족해야 한다.

- 새 동작과 회귀 위험을 검증하는 의미 있는 테스트가 있다.
- 테스트가 0개인 상태에서 `--passWithNoTests`가 성공한 결과를 기능 검증 PASS로 보지 않는다.
- 플랫폼 DOM 접근은 `src/platforms/**` 어댑터 경계에 둔다.
- Chrome 확장 기능은 build 성공 외에 unpacked extension 수동 smoke test를 수행한다.
- manifest 권한과 host permission 증가는 인수 조건, 보안 검토, ADR 또는 명시적 승인이 필요하다.
- CI의 Validate check와 보호 브랜치 정책이 원격 병합의 최종 게이트다.

## 8. 보안 및 데이터 게이트

- API Key, OAuth/GitHub 토큰, 개인정보, 문제 원문 등 민감한 값을 코드, fixture, 로그,
  오류 메시지, 분석 이벤트, handoff에 넣지 않는다.
- 비밀 값은 저장소에 커밋하지 않고 `.env.example`에는 이름과 용도만 기록한다.
- 외부 전송과 업로드는 대상 데이터와 목적을 문서화하고 사용자의 명시적 확인 뒤 실행한다.
- 자동 업로드는 기본 비활성화한다.
- 새 권한은 최소 권한 원칙을 적용하고 수집 데이터, 전송 대상, 보존 방식을 기록한다.
- 스키마 변경은 호환성, 마이그레이션, rollback 또는 복구 방법을 함께 설계하고 검증한다.
- 보안 결함이나 비밀 노출 가능성이 있으면 작업을 중단하고 사용자와 기획 역할에 보고한다.

## 9. 완료 정의

작업은 다음 조건을 모두 만족할 때 완료다.

- 승인된 범위와 인수 조건을 충족한다.
- 관련 테스트가 존재하고 전체 검증이 통과한다.
- 필요한 Chrome 수동 검증과 보안 검토가 완료됐다.
- README, 아키텍처, 데이터 모델, ADR, KPI 또는 트러블슈팅 문서 영향이 반영됐다.
- 잔여 위험과 후속 작업이 명시됐다.
- 기획 역할이 증거를 확인하고 상태를 종료로 변경했다.

## 10. 작업 저장소 경로 불변 규칙

- 사용자가 지정했거나 기존 Git 저장소로 확인된 경로를 해당 작업의 기준 저장소로 사용한다.
- 권한, 샌드박스 또는 도구 제약이 있어도 저장소를 임의로 다른 경로에 복제, 복사, 이동하거나
  새 저장소로 대체하지 않는다. 필요한 경우 기준 경로에 대한 승인을 요청하고 작업을 멈춘다.
- 모든 파일 변경과 Git 명령 전에 `git rev-parse --show-toplevel`로 기준 저장소 경로가
  사용자가 지정한 경로와 일치하는지 확인한다.
- 임시 복사본이나 복구용 스냅샷은 사용자의 명시적 승인 없이 작업 기준 또는 Git 원격의
  소스로 사용하지 않는다.
- 경로 불일치를 발견하면 새 작업을 진행하지 않고 두 저장소의 HEAD, branch, remote와
  working tree 차이를 먼저 보고한다.
