{
  "name": "codearchive",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm --prefix apps/dashboard ci && npm --prefix apps/extension ci",
    "dev": "npm --prefix apps/dashboard run dev",
    "build": "npm --prefix apps/dashboard run build && npm --prefix apps/extension run build",
    "test:extension": "npm --prefix apps/extension test",
    "test:dashboard": "npm --prefix apps/dashboard test",
    "test": "npm run test:dashboard && npm run test:extension"
  }
}
