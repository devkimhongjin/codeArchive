# CodeArchive 프로젝트 제어 문서

> 상태: 초안(Draft)  
> 현재 마일스톤: M0 — 기반 정렬  
> 일정 기준: 1주 스프린트, 시작일과 출시일은 미확정

이 문서는 CodeArchive 개발의 범위, 운영 규칙, 의사결정 상태를 한곳에서 확인하기 위한 프로젝트 제어 문서다. 기능의 상세 근거는 `docs/proposal/codearchive-proposal.md`, 실행 순서는 `docs/project/roadmap.md`, 작업 단위는 `docs/project/backlog.md`를 기준으로 한다.

## 1. 프로젝트 목표

SWEA, 프로그래머스, 정올, LeetCode의 코딩테스트 풀이를 수집하고 다음 학습 정보를 함께 관리하는 Chrome Extension을 만든다.

- 문제 정보, 제출 코드, 제출 결과 자동 수집
- 기존 풀이 가져오기와 중복 병합
- 풀이 과정, 오답 원인, 복잡도 기록
- AI 활용 수준, 목적, 이해도, 재풀이 가능성 기록
- 소스 파일, README, Markdown, JSON 내보내기
- 사용자 API Key 기반 AI 코드 리뷰
- Notion 학습 노트와 GitHub 포트폴리오 연결
- AI 활용과 오답 이력을 고려한 복습 및 통계

단기 목표는 제안서의 MVP를 검증 가능한 작은 단위로 구현하는 것이다. 정올, LeetCode, 서버 동기화와 고급 자동화는 MVP 이후 범위로 둔다.

## 2. 현재 상태

현재 상태는 **M0 — 기반 정렬**이다.

- Vue 3, TypeScript, Vite 기반 Chrome Extension 골격과 기본 검증 명령은 존재한다.
- 핵심 데이터 모델, 플랫폼 어댑터, 저장소 계층, 실제 기능 테스트는 아직 완료로 판정하지 않는다.
- 아키텍처와 데이터 모델 문서에는 작성 예정 항목이 남아 있다.
- 진행률 백분율은 사용하지 않는다. 근거가 확인된 완료 작업만 `Done`으로 표시한다.
- 특정 스프린트의 시작일, 종료일, 출시일은 담당자가 확정하기 전까지 기록하지 않는다.

## 3. 역할과 책임

### 기획

- 제안서를 기준으로 범위와 우선순위를 관리한다.
- 스프린트 목표와 수용 기준을 조정한다.
- 추가 기능 제안, 변경 요청, 결정 대기 항목을 검토한다.
- 변경이 KPI, 보안, 일정, 기존 데이터와 호환되는지 확인한다.

### 구현

- 승인된 작업과 수용 기준에 맞춰 코드를 작성한다.
- 플랫폼 DOM 접근을 플랫폼 어댑터 안으로 격리한다.
- 마이그레이션, 오류 처리, 보안 영향을 구현에 포함한다.
- 작업 범위 밖의 설계 변경이 필요하면 임의 확장하지 않고 변경 요청을 남긴다.

### 검증

- 기획의 수용 기준과 제안서 성공 기준을 테스트로 연결한다.
- lint, format, typecheck, test, production build를 검증한다.
- 테스트가 없는 성공, 수동 확인만 한 성공, 재현할 수 없는 성공은 완료로 인정하지 않는다.
- 플랫폼 DOM 변경, 저장소 마이그레이션, 외부 전송과 권한을 위험 기반으로 검증한다.

### 기록

- 트러블슈팅, 기술 스택, README, KPI 측정 결과와 ADR을 유지한다.
- 결정의 배경, 대안, 영향과 후속 작업을 기록한다.
- 완료 주장에는 명령 결과, 테스트, 스크린샷 또는 재현 절차 중 적절한 근거를 연결한다.

## 4. 1주 스프린트 운영

모든 스프린트는 1주 단위로 운영하되 달력 날짜는 별도로 확정한다.

1. 스프린트 계획: 기획이 목표와 후보 작업을 제안하고 네 역할이 수용 기준과 의존성을 확인한다.
2. 작업 착수: `Proposed` 작업 중 결정 대기와 의존성이 해소된 항목만 `Ready`로 변경한다.
3. 구현과 기록: 구현은 작은 변경 단위로 진행하고 기록은 설계 결정과 문제 해결을 같은 스프린트 안에 반영한다.
4. 지속 검증: 검증 역할은 구현 완료 후에만 참여하지 않고 수용 기준, fixture와 실패 사례를 먼저 검토한다.
5. 스프린트 리뷰: 완료 정의를 만족한 결과만 `Done`으로 판정하고 KPI 근거를 남긴다.
6. 회고와 재계획: 미완료 작업은 자동 완료 처리하지 않고 원인과 다음 선택을 기록한 뒤 백로그로 되돌린다.

권장 WIP 제한은 구현 중 작업 1개, 검증 중 작업 1개다. 실제 처리량이 쌓이기 전에는 스프린트별 예상 완료량이나 속도를 확정하지 않는다.

## 5. 작업 상태

- `Proposed`: 초안이며 아직 착수 승인 전
- `Decision Pending`: 중요한 선택이나 외부 확인이 필요함
- `Ready`: 의존성, 수용 기준, 검증 방법이 준비됨
- `In Progress`: 현재 구현 중
- `In Verification`: 구현이 끝나 독립 검증 중
- `Blocked`: 구체적인 장애물 때문에 진행할 수 없음
- `Done`: 완료 정의를 모두 충족함

상태 변경에는 담당 역할, 변경일, 근거 링크가 필요하다. 날짜와 근거가 없는 완료 표시는 허용하지 않는다.

현재 작업:

| 작업      | GitHub Issue                                                | 상태 | 변경일     | 근거                                                                                                                        |
| --------- | ----------------------------------------------------------- | ---- | ---------- | --------------------------------------------------------------------------------------------------------------------------- |
| TASK-0001 | [#1](https://github.com/devkimhongjin/codeArchive/issues/1) | Done | 2026-07-25 | [최종 검증 PASS](docs/verification/2026-07-25-TASK-0001-recheck-2.md)                                                       |
| TASK-0002 | [#2](https://github.com/devkimhongjin/codeArchive/issues/2) | Done | 2026-07-26 | [최종 검증 PASS](docs/verification/2026-07-26-TASK-0002.md), [기록 완료](.agents/handoffs/TASK-0002-records-to-planning.md) |

GitHub Issue를 작업 상태와 토론의 원격 기준으로 사용하고, 상세 수용 기준과 의존성은
`docs/project/backlog.md`에서 관리한다. 두 위치가 충돌하면 기획 역할이 먼저 동기화한다.

TASK-0002의 `Done`은 M0 플랫폼 어댑터 계약과 합성 fixture 범위에 한정한다. Chrome
unpacked extension smoke는 `Not Run`이고 실제 SWEA DOM·로그인·편집기·제출 UI 및 제품
KPI는 검증하지 않았다. 이 항목들은 실제 플랫폼 연결과 권한을 다루는 M2의 필수 검증
게이트로 이관하며, TASK-0002의 합성 계약 PASS를 실제 사이트 호환성으로 해석하지 않는다.

## 6. 결정 대기

| ID       | 결정 항목                                  | 필요한 근거                                        | 영향                      |
| -------- | ------------------------------------------ | -------------------------------------------------- | ------------------------- |
| DEC-0001 | MVP의 정확한 1차 배포 경계                 | Must Have 목록의 사용자 가치, 구현 비용, 보안 검토 | 마일스톤 범위와 출시 판단 |
| DEC-0002 | IndexedDB 접근 계층과 스키마 버전 전략     | 데이터 모델, 마이그레이션/롤백 시험                | 데이터 호환성과 복구      |
| DEC-0004 | AI 제공자와 API Key 저장·호출 방식         | 비용, Chrome 보안 제약, 전송 고지                  | AI 리뷰 범위와 보안       |
| DEC-0005 | GitHub 인증과 수동 업로드 방식             | 최소 권한, 토큰 저장 정책, UX                      | manifest 권한과 보안      |
| DEC-0006 | Notion MVP를 Markdown 복사로 제한할지 여부 | 제안서 MVP 제외 범위와 사용자 검증                 | 일정과 외부 연동 범위     |

결정 전에는 관련 기능을 완료로 간주하지 않는다. 대기 항목을 해소할 때는 ADR과 영향을 받는 백로그를 함께 갱신한다.

### 해소된 결정

| ID       | 결정일     | 결정                                                                                                     | 근거                                                                                             | 후속 기록                                                           |
| -------- | ---------- | -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| DEC-0003 | 2026-07-26 | 실제 사이트 원본 대신 최소 합성·비식별 DOM fixture를 사용하고 실제 DOM 캡처는 별도 승인 전까지 제외한다. | 재현성 확보, 개인정보·문제 원문·비공개 테스트 데이터의 저장 방지, selector 변경 실패의 독립 재현 | [ADR-0002 Accepted](docs/adr/ADR-0002-platform-adapter-fixtures.md) |

## 7. 변경 관리

범위, 데이터 모델, 외부 권한, 보안 정책, KPI 또는 마일스톤 순서를 바꾸는 제안은 변경 요청으로 관리한다.

변경 요청에는 다음 내용을 포함한다.

- 변경하려는 내용과 이유
- 사용자 가치 또는 해결할 문제
- 제안서 MVP 및 우선순위와의 관계
- 영향을 받는 작업, 데이터, 권한, 테스트와 문서
- 보안·개인정보·플랫폼 정책 영향
- 수용 기준과 롤백 또는 호환 계획
- 선택 가능한 대안과 미변경 시 영향

기획이 요청을 `Accept`, `Reject`, `Defer`, `Need Evidence` 중 하나로 판정한다. 승인된 변경은 먼저 제안서 또는 ADR에 반영하고, roadmap과 backlog를 갱신한 다음 구현한다. 긴급 결함 수정도 사후에 같은 근거를 남긴다.

## 8. 완료 정의

작업은 다음 조건을 모두 만족할 때만 `Done`이다.

- 승인된 범위와 수용 기준을 충족한다.
- 구현과 함께 정상, 경계, 실패 경로 테스트가 추가되거나 테스트 불필요 사유가 검증 기록에 남는다.
- lint, format check, typecheck, test, production build가 성공한다.
- Chrome Extension에서 필요한 수동 시나리오가 재현 가능하게 기록된다.
- 새 권한과 외부 전송은 최소 권한, 사용자 고지, 민감 정보 비노출을 확인한다.
- 데이터 변경은 마이그레이션과 이전 버전 호환 또는 명시적 전환 정책이 있다.
- README, 아키텍처, 데이터 모델, ADR, KPI 중 영향을 받은 문서가 갱신된다.
- 알려진 제한, 미해결 위험과 후속 작업이 백로그에 연결된다.
- 검증 역할이 구현 역할의 주장과 근거를 독립적으로 확인한다.

마일스톤 완료는 포함된 필수 작업이 모두 `Done`이고 해당 마일스톤 종료 기준을 충족했을 때만 선언한다.

## 9. 제안서 성공 기준

아래 수치는 제품 성공 판단의 목표값이며 현재 달성값이 아니다.

- 자동 수집: 주요 문제·코드·제출 결과 수집 성공률 90% 이상, 언어 감지 95% 이상
- AI 기록: 제출 완료 후 5초 이내 기록, 미사용과 미기록 구분, 세션별 독립 기록
- 내보내기: 확장자 정확도 100%, 다운로드 성공률 99% 이상
- 가져오기: JSON 성공률 99% 이상, 파일 인식과 중복 감지 95% 이상
- 보안: API Key의 로그·출력 파일 미노출, 외부 업로드 전 확인, 공개 범위 통제
- 사용성: 제출부터 기록 완료까지 10초 이내, 수동 등록 1분 이내, 브라우저 재실행 후 기록 유지

측정 표본, 환경, 성공/실패 정의가 합의되기 전에는 수치를 달성했다고 기록하지 않는다.
